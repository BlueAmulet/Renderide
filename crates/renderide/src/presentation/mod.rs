//! Presentation split: desktop window vs OpenXR mirror + headset submission.
//!
//! The concrete loop lives in [`crate::app`]. Launch mode is [`crate::launch_mode::RenderMode`].

pub use crate::launch_mode::RenderMode;
