[package]
name = "renderide"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1.14", features = ["derive"] }
nalgebra = { version = "0.34.1", features = ["convert-bytemuck"] }
